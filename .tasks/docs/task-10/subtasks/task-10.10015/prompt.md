Implement subtask 10015: Create GitHub Actions quality.yml workflow with Cleo linting for all language stacks

## Objective
Create .github/workflows/quality.yml that triggers on pull_request. Run Cleo-driven linting: clippy + rustfmt for Rust services, eslint + biome.js for Node/TypeScript services, go vet + golangci-lint for Go services. Fail the workflow if any linter reports errors.

## Steps


## Validation
Verify the subtask outcomes.