name: implement-task-6
metadata:
  task_id: 6
  agent: nova
  stack: Node.js 20+/Elysia 1.x/Effect 3.x
  cli: claude
  model: claude-opus-4-6
  depends_on_tasks: [1]

args:
  task_dir: {}
  repo_url: {}
  branch_name:
    default: "task-6/feature-branch"
  agent:
    default: "nova"
  cli:
    default: "claude"
  model:
    default: "claude-opus-4-6"
  fallback_cli:
    default: "codex"
  fallback_model:
    default: "gpt-5.2-codex"
  max_implementation_retries:
    default: 10
  max_quality_retries:
    default: 5

steps:
  - id: setup
    command: >
      git clone ${repo_url} work && cd work &&
      git checkout -b ${branch_name}

  - id: implement-subtask-6001
    depends_on: [setup]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6001/prompt.md)" --model ${model}

  - id: validate-subtask-6001
    depends_on: [implement-subtask-6001]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6002
    depends_on: [validate-subtask-6001]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6002/prompt.md)" --model ${model}

  - id: validate-subtask-6002
    depends_on: [implement-subtask-6002]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6003
    depends_on: [validate-subtask-6001]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6003/prompt.md)" --model ${model}

  - id: validate-subtask-6003
    depends_on: [implement-subtask-6003]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6004
    depends_on: [validate-subtask-6002, validate-subtask-6003]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6004/prompt.md)" --model ${model}

  - id: validate-subtask-6004
    depends_on: [implement-subtask-6004]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6005
    depends_on: [validate-subtask-6004]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6005/prompt.md)" --model ${model}

  - id: validate-subtask-6005
    depends_on: [implement-subtask-6005]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6006
    depends_on: [validate-subtask-6005]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6006/prompt.md)" --model ${model}

  - id: validate-subtask-6006
    depends_on: [implement-subtask-6006]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6007
    depends_on: [validate-subtask-6005]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6007/prompt.md)" --model ${model}

  - id: validate-subtask-6007
    depends_on: [implement-subtask-6007]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6008
    depends_on: [validate-subtask-6004]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6008/prompt.md)" --model ${model}

  - id: validate-subtask-6008
    depends_on: [implement-subtask-6008]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6009
    depends_on: [validate-subtask-6003]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6009/prompt.md)" --model ${model}

  - id: validate-subtask-6009
    depends_on: [implement-subtask-6009]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6010
    depends_on: [validate-subtask-6003]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6010/prompt.md)" --model ${model}

  - id: validate-subtask-6010
    depends_on: [implement-subtask-6010]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6011
    depends_on: [validate-subtask-6003]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6011/prompt.md)" --model ${model}

  - id: validate-subtask-6011
    depends_on: [implement-subtask-6011]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6012
    depends_on: [validate-subtask-6003]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6012/prompt.md)" --model ${model}

  - id: validate-subtask-6012
    depends_on: [implement-subtask-6012]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6013
    depends_on: [validate-subtask-6009, validate-subtask-6010, validate-subtask-6011, validate-subtask-6012]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6013/prompt.md)" --model ${model}

  - id: validate-subtask-6013
    depends_on: [implement-subtask-6013]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6014
    depends_on: [validate-subtask-6001]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6014/prompt.md)" --model ${model}

  - id: validate-subtask-6014
    depends_on: [implement-subtask-6014]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: implement-subtask-6015
    depends_on: [validate-subtask-6005, validate-subtask-6006, validate-subtask-6007, validate-subtask-6008, validate-subtask-6013, validate-subtask-6014]
    retry: { max: "${max_implementation_retries}" }
    command: >
      ${cli} -p "$(cat ${task_dir}/subtasks/task-6.6015/prompt.md)" --model ${model}

  - id: validate-subtask-6015
    depends_on: [implement-subtask-6015]
    retry: { max: "${max_quality_retries}" }
    command: >
      cd work && echo "No type checker configured" && echo "No test runner configured" && echo "No linter configured"

  - id: integration-validation
    depends_on: [validate-subtask-6001, validate-subtask-6002, validate-subtask-6003, validate-subtask-6004, validate-subtask-6005, validate-subtask-6006, validate-subtask-6007, validate-subtask-6008, validate-subtask-6009, validate-subtask-6010, validate-subtask-6011, validate-subtask-6012, validate-subtask-6013, validate-subtask-6014, validate-subtask-6015]
    command: >
      cd work && bun tsc --noEmit && bun test --run && bun lint

  - id: acceptance-check
    depends_on: [integration-validation]
    command: >
      ${cli} -p "Review changes against ${task_dir}/acceptance.md. Output PASS or FAIL." --model ${model}

  - id: create-pr
    depends_on: [acceptance-check]
    command: >
      cd work && git add -A &&
      git commit -m "feat: task-6" &&
      git push origin ${branch_name} &&
      source "${WORKSPACE:-.}/scripts/scm-dispatch.sh" &&
      scm_create_pr "feat: task-6" "$(cat ${task_dir}/task.md)" "main"